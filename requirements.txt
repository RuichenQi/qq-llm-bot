websockets>=13.0
httpx>=0.27.0
python-dotenv>=1.0.1
aiosqlite>=0.20.0
Pillow>=10.0.0
# Optional — only needed for file-ingest. The bot degrades gracefully if
# they're missing (PDFs / docx will return a "missing dep" message instead
# of crashing). Comment out either if you don't need that format.
pypdf>=4.0.0
python-docx>=1.1.0
